# BlockDrive Deployment Guide

## Hugging Face Spaces (Docker)
Push the contents of THIS folder (ss-server-main) to your HF Space repo.
The Dockerfile, start.sh, and nginx.conf are already configured.

## Local WiFi
Run: python start_local.py
Then share your local IP with others on the same network.
